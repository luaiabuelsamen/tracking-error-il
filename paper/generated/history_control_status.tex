The seed-1 comparison was run once and interrupted after 9 complete pairs (19 rollouts, 5 ending with a gripper overload error): the gripper servo stopped following jaw commands in several rollouts and the overhead camera was found displaced relative to the earlier sessions, so its outcomes are retained (Table~\ref{tab:history}) but the comparison is not complete and no confirmatory test is assigned to it. The other registered comparison has not been run.
